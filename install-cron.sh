# #!/bin/bash
# sudo apt-get install cron
# echo -e "$(sudo crontab -u root -l)\n* * * * * curl 'https://backend.zevbit.com/api/twilio/sync/webhook'" | sudo crontab -u root -
# echo -e "$(sudo crontab -u root -l)\n* * * * * curl 'https://backend.zevbit.com/api/twilio/monthly/number/charge'" | sudo crontab -u root -
